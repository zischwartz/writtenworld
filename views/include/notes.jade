

for note in notes
  li.note(class=note.type) 
    if note.type != 'own'
      b #{note.type} by #{note.fromLogin}:
    else
      b You Wrote:
    blockquote !{note.contents}
    i #{note.date}
    a.btn.pull-right.trigger(data-action='goto', data-payload='#{note.x}x#{note.y}') Go To 

- if (! notes.length)
  p.well No Notes, Sorry.
