!!! 5
html(lang="en")
  head
    title= title 
    link(rel="stylesheet", href="/css/leaflet.css")

    link(rel="stylesheet", href="/css/bootstrap.css")
    link(rel="stylesheet", href="/css/font-awesome.css")
    link(rel="stylesheet", href="/css/colorpicker.css")
    
    meta(name = "viewport", content = "width = device-width, initial-scale = 1.0,  user-scalable = no")
    meta(name="apple-mobile-web-app-capable", content="yes") 

    script
      initialWorldId="#{initialWorldId}";
      personalWorldId="#{personalWorldId}";
      mainWorldId = "#{mainWorldId}";
      isAuth = #{isAuth};
      worldSpec = !{worldSpec};
      initialPos = "#{initialPos}";
      pageStartLoad = new Date().getTime();
      
    script(src="/nowjs/now.js")
    script(src="/assets/js/application.js")
    
    style(id="dynamicStyles")
      
    script(type="text/javascript")
      var _gaq = _gaq || [];
        _gaq.push(['_setAccount', 'UA-27491690-1']);
        _gaq.push(['_setDomainName', 'writtenworld.org']);
        _gaq.push(['_trackPageview']);

        (function() {
          var ga = document.createElement('script'); ga.type = 'text/javascript'; ga.async = true;
          ga.src = ('https:' == document.location.protocol ? 'https://ssl' : 'http://www') + '.google-analytics.com/ga.js';
          var s = document.getElementsByTagName('script')[0]; s.parentNode.insertBefore(ga, s);
        })();
 

body
  include include/nav.jade
  div#notes.span4
    div.loading
  div(id="messages") 
  div#loadingIndicator
    div.progress.progress-success
      div.bar LOADING
  div#errorIndicator
    div.progress.progress-danger
      div.bar NOT CONNECTED!
        h5  Maybe reload the page if this doesn't go away in a few seconds.

  div(id="map")

  div(id="inputWrap")
    input(id="input", type="text", autocorrect="off", autocapitalize="off")
  include include/modals.jade

